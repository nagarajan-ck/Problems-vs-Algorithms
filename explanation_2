The program akes O(log(n)) as it implements a varied form of binary search and length of list halves at every stage, n is the length of list.

Space complexity is O(n) where n is the size of the list
